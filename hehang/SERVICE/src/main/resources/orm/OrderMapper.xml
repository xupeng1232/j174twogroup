<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.mapper.IOrderMapper">

   <insert id="add" parameterType="orderBean" useGeneratedKeys="true" keyProperty="id">
       insert into t_order values (null,#{fk_product},#{price})
   </insert>

    <update id="update">
        update t_order set o_price=20 where o_id = 12
    </update>
  
</mapper>